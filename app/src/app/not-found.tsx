export default function NotFound() {
  // This not found only happen when we could not find a file,
  // else it'll be handled in ./[locale]/[...notFound]/page.tsx
  return <main>not found global</main>;
}
